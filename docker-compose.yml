# Mini-NVR Docker Compose

services:
  nvr:
    build: .
    container_name: mini-nvr
    restart: always
    env_file: .env
    ports:
      - "${WEB_PORT:-2126}:${WEB_PORT:-2126}"
    volumes:
      - ./recordings:/recordings
      - ./logs:/logs
